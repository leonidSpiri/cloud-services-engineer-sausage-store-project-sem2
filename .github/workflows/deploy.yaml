name: Sausage Store Deploy

on:
  push:
    branches:
      - main

permissions:
  contents: read

env:
  DOCKERHUB_NAMESPACE: lspiridonov
  BACKEND_IMAGE: lspiridonov/sausage-backend
  FRONTEND_IMAGE: lspiridonov/sausage-frontend
  BACKEND_REPORT_IMAGE: lspiridonov/sausage-backend-report
  HELM_CHART_VERSION: 0.1.${{ github.run_number }}-${{ github.run_attempt }}
  NEXUS_HELM_REPO: ${{ secrets.NEXUS_HELM_REPO }}
  NEXUS_HELM_REPO_USER: ${{ secrets.NEXUS_HELM_REPO_USER }}
  NEXUS_HELM_REPO_PASSWORD: ${{ secrets.NEXUS_HELM_REPO_PASSWORD }}
  SAUSAGE_STORE_NAMESPACE: ${{ secrets.SAUSAGE_STORE_NAMESPACE }}
  KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
  VAULT_TOKEN: ${{ secrets.VAULT_TOKEN }}

jobs:
  # Сборка и пуш образов в докерхаб:
  build_and_push_to_docker_hub:
    name: Push Docker image to DockerHub
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        # Получение исходного кода из репозитория:
        uses: actions/checkout@v4
      - name: Set up Docker Buildx
        # Установка сборщика контейнеров Docker:
        uses: docker/setup-buildx-action@v3
      - name: Login to Docker
        # Авторизация на Docker Hub:
        uses: docker/login-action@v3
        # При помощи with передаём в action параметры username и password:
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Push Backend to DockerHub
        # Одновременный билд и пуш образа в Docker Hub:
        uses: docker/build-push-action@v6
        with:
          # Параметр context: ./backend/ указывает, что нужный Dockerfile
          # находится в ./backend/
          context: ./backend/
          # Параметр push: true указывает, что образ нужно не только собрать,
          # но и отправить на Docker Hub:
          push: true
          # В параметре tags задаётся название и тег для образа.
          # Для каждого пересобранного образа
          # устанавливаем тег latest, чтобы потом
          # на сервере и в docker-compose.yml не указывать версию образа:
          tags: |
            ${{ env.BACKEND_IMAGE }}:latest
            ${{ env.BACKEND_IMAGE }}:${{ github.sha }}
          build-args: |
            VERSION=${{ github.sha }}
      - name: Push Frontend to DockerHub
        uses: docker/build-push-action@v6
        with:
          context: ./frontend/
          push: true
          tags: |
            ${{ env.FRONTEND_IMAGE }}:latest
            ${{ env.FRONTEND_IMAGE }}:${{ github.sha }}
      - name: Push Backend-report to DockerHub
        uses: docker/build-push-action@v6
        with:
          context: ./backend-report/
          push: true
          tags: |
            ${{ env.BACKEND_REPORT_IMAGE }}:latest
            ${{ env.BACKEND_REPORT_IMAGE }}:${{ github.sha }}
  # Упаковываем helm-чарт и пушим его в Nexus
  add_helm_chart_to_nexus:
    name: Add Helm Chart To Nexus
    runs-on: ubuntu-latest
    needs: build_and_push_to_docker_hub

    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.14.4

      - name: Lint Helm chart
        run: helm lint ./sausage-store-chart

      - name: Package Helm chart
        run: |
          mkdir -p packaged-chart
          helm package ./sausage-store-chart \
            --destination packaged-chart \
            --version "$HELM_CHART_VERSION" \
            --app-version "${{ github.sha }}"

      - name: Upload Helm chart to Nexus
        run: |
          chart_package="$(ls packaged-chart/*.tgz | head -n 1)"
          curl --fail --silent --show-error \
            --user "${NEXUS_HELM_REPO_USER}:${NEXUS_HELM_REPO_PASSWORD}" \
            --upload-file "${chart_package}" \
            "${NEXUS_HELM_REPO%/}/"

  # Деплоим приложения в кластер Kubernetes с помощью helm-чарт
  deploy_helm_chart_to_kubernetes:
    name: Deploy Helm Chart To Kubernetes
    runs-on: ubuntu-latest
    needs: add_helm_chart_to_nexus

    steps:
      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.14.4

      - name: Set up kubectl
        uses: azure/setup-kubectl@v4

      - name: Add Nexus Helm repository
        run: |
          helm repo add nexus "$NEXUS_HELM_REPO" \
            --username "$NEXUS_HELM_REPO_USER" \
            --password "$NEXUS_HELM_REPO_PASSWORD"
          helm repo update

      - name: Configure kubeconfig
        run: |
          if [ -z "$KUBE_CONFIG" ]; then
            echo "KUBE_CONFIG secret is empty"
            exit 1
          fi
          
          if [ -z "$SAUSAGE_STORE_NAMESPACE" ]; then
            echo "SAUSAGE_STORE_NAMESPACE secret is empty"
            exit 1
          fi
          
          mkdir -p "$HOME/.kube"
          printf '%s' "$KUBE_CONFIG" > "$HOME/.kube/config"
          chmod 600 "$HOME/.kube/config"
          kubectl config set-context --current --namespace="$SAUSAGE_STORE_NAMESPACE"

      - name: Deploy Helm chart to Kubernetes
        run: |
          helm template sausage-store nexus/sausage-store \
            --version "$HELM_CHART_VERSION" \
            --namespace "$SAUSAGE_STORE_NAMESPACE" \
            --set backend.image="${BACKEND_IMAGE}:${GITHUB_SHA}" \
            --set frontend.image="${FRONTEND_IMAGE}:${GITHUB_SHA}" \
            --set backend-report.image="${BACKEND_REPORT_IMAGE}:${GITHUB_SHA}" \
            >/tmp/sausage-store-rendered.yaml
          
          helm upgrade --install sausage-store nexus/sausage-store \
            --version "$HELM_CHART_VERSION" \
            --namespace "$SAUSAGE_STORE_NAMESPACE" \
            --create-namespace \
            --wait \
            --timeout 10m \
            --set backend.image="${BACKEND_IMAGE}:${GITHUB_SHA}" \
            --set frontend.image="${FRONTEND_IMAGE}:${GITHUB_SHA}" \
            --set backend-report.image="${BACKEND_REPORT_IMAGE}:${GITHUB_SHA}"
